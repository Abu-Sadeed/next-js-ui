/** @type {import('next').NextConfig} */

const nextConfig = {
    eslint: {
        dirs: ["."]
    },
    poweredByHeader: false,
    trailingSlash: true,
    basePath: "",
    reactStrictMode: true
};

export default nextConfig;
